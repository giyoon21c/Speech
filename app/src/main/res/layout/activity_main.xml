<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.myapplication.MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewThreadCount"
        android:layout_alignParentTop="true"
        android:textSize="20dp"
        android:gravity="center"
        android:layout_marginBottom="10dp"
        android:padding="5dp"
        android:text="text view..."/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStartCallback"
        android:layout_below="@id/textViewThreadCount"
        android:text="Start callback"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStopCallback"
        android:layout_below="@id/buttonStartCallback"
        android:text="Stop callback"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStartService"
        android:layout_below="@id/buttonStopCallback"
        android:text="Start service"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStopService"
        android:layout_below="@id/buttonStartService"
        android:text="Stop service"/>

</RelativeLayout>
